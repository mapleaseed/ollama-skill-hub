name: ollama_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - fastapi
      - httpx
      - python-multipart
      - requests
      - pyyaml
      - uvicorn
